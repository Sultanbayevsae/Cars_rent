spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cars_db
    username: postgres
    password: linux
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  freemarker:
    template-loader-path: classpath:/templates/
    suffix: .ftl
  output:
    ansi:
      enabled: ALWAYS
logging:
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(${PID}){magenta} --- %clr(%logger{36}){cyan} : %m%n"

jwt:
  secret:
    key: supercalifragilisticexpialidociousNewChemicalUndisputableLeadershipOfWorldLeaders202510
    access-exp-ms: 9000000
    refresh-exp-ms: 12096000000
